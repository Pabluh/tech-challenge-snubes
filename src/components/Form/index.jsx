import styles from "./styles.module.scss";

export const Form = () => {
  return (
    <form className={styles.content}>
      <span>Find inbound call centers for your company</span>
    </form>
  );
};
